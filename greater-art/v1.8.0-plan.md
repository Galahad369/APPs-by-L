# v1.8.0 Plan — 14 items

## What's already wired (no code change needed, just verify)
1. ReplayGain — prefs, PlaybackService.applyGain, ReplayGain.kt
2. Folder exclusions — prefs, MediaScanner.scan(excludedFolders), Settings UI
3. Duplicate finder — DuplicateDialog.kt, Settings button
4. System equalizer — Settings button, ACTION_DISPLAY_AUDIO_EFFECT_CONTROL_PANEL
5. Backup/restore JSON — AppPreferences exportBackup/restoreBackup
6. Home-screen widget — PlaybackWidget.kt, service updates on media change
8. A-B repeat — PracticeLoop + NowPlayingScreen UI
14. FakeAdInterstitial — FakeAdInterstitial.kt, Settings toggle, intent handling

## Needs code work
7. Android Auto — MediaLibrarySession needs BrowserRoot/loadChildren for car browsing + voice metadata
9. Splash — replace splash_mark.xml with properly inset vector (not just cropped launcher icon)
10. Waveform depth — 72-bar Canvas needs stronger dynamic range, gradient fill, loading/cached states
11. Sleep toggle visibility — NowPlayingScreen must hide sleep controls when showSleepControl=false, resize controls
12. Red X brightness — increase alpha 0.90→1.0, stroke 0xCC→0xFF
13. Thumbnail scheduling — fix warmup/lookahead job separation per handoff #23/#31

## Order
9 → 10 → 12 → 11 → 13 → 7 → verify all, test, build, sign, document, commit, push

## Device testing boundary
Android Auto, widget, equalizer availability, overlay gestures, sleep timer — require phone-side testing. State honestly.
