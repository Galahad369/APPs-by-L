<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- FileProvider still grants only the exact URI placed in the share Intent. -->
    <external-path name="download_media" path="Download/" />
    <cache-path name="shared_lists" path="shared_lists/" />
</paths>
